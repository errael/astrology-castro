These tests generate files that are not expected to run in Astrolog.
They are intended to detect changes in grammar and code generation.
The output format facilitates seeing any changes.
They do not execute at least because of line breaks.
